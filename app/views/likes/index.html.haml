.container
  .jumbotron
    %h1
      Likes

  - if @researches.blank?
    %h2
      = @current.first_name
      has no likes!
  - else
    %table.table-striped.table-responsive
      %thead
        %tr
          %th
            = sort_link(@search, :name, "Name")
          %th
            = sort_link(@search, :location, "Location")
          %th
            = sort_link(@search, :researcher_name, "Researcher")


      %tbody
        - @researches.each do |research|
          %tr
            %td
              = link_to research.name, research
            %td
              = link_to research.location, research
            %td
              = link_to research.researcher.name, research.researcher



- @researches.each do |research|
  = link_to research.name, research
