.field
  = f.attribute_fields do |a|
    = a.attribute_select associations: [:category]
  = f.predicate_select
  = f.value_fields do |v|
    = v.text_field :value
  = link_to "Remove", '#', class: "btn btn-primary remove_fields"
